{
  "name": "patent-crm-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "npm-run-all --parallel start:server start:client",
    "start:server": "cd server && npm start",
    "start:client": "cross-env PORT=3001 BROWSER=none REACT_APP_API_URL=http://localhost:5000 npm start"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5"
  }
}
